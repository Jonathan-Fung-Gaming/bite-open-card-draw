# UX/UI Tournament Readiness Checklist - 2026-07-05

Use this checklist to close the issues from the July 5 UX/UI validation pass. The companion
execution plan is `docs/ux-ui-tournament-readiness-remediation-plan-2026-07-05.md`.

Codex routing note: this is the active UX/UI remediation checklist. Use it with
`docs/codex-current-brief.md` and the active phase section of the companion plan. Older checklists
are historical unless the user explicitly references them.

Do not check an item until:

- the issue has been fixed or explicitly accepted as-is by the user,
- the fix has route-level evidence or a targeted automated test,
- relevant screenshots or notes are recorded in the phase summary,
- lint, typecheck, tests, build, and e2e gates have been run where applicable.

The product behavior source of truth remains `docs/product-spec.md` and
`docs/pump_open_stage_repo_validation_checklist.md`.

## Image And Chart Art

| Done | ID      | Severity | Phase | Issue                                                                                                                            | Closure evidence                                                                                                                                                                                                                                                                                   |
| ---- | ------- | -------- | ----- | -------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [ ]  | UXR-001 | P1       | 0     | Live production shows fallback/placeholder chart images even though local cached images validate.                                | Phase 0 classified this as a deployment artifact/stale production artifact: sampled live cache PNG returned 404 and live `/stage` plus `/charts` used fallback art. Keep open until a newer deployment serves `/chart-images/cache/*` and route evidence proves cache art renders.                 |
| [x]  | UXR-002 | P1       | 3     | Chart-image components rely on precomputed fallback paths; broken non-fallback cache URLs do not visibly recover at render time. | Phase 3 added `ChartArtImage` render-time fallback with hydration-missed 404 recovery; `rtk npm run test:e2e` passed forced cache-404 evidence for `/stage`, `/charts`, `/vote`, and `/results` screenshots named `uxr-002-*image-fallback.png`.                                                   |
| [x]  | UXR-003 | P1       | 3     | Mobile ballot/chart/result cards crop and darken chart art enough that artwork is difficult to inspect.                          | Phase 3 replaced dim mobile background art with visible image bands and metadata assertions; `rtk npm run test:e2e` passed mobile `/vote`, `/charts`, and `/results` evidence including `pfr-mobile-vote-ballot.png`, `uxr-003-*-mobile-charts-set-*.png`, and `uxr-004-mobile-results-final.png`. |
| [x]  | UXR-004 | P2       | 3     | Long selected chart names and artists truncate in primary public result cards.                                                   | Phase 3 removed selected-result title/artist clamps; `rtk npm run test:e2e` passed selected-card readability checks and captured `uxr-004-mobile-results-corrected-long-name.png` with the longest available real corrected chart name.                                                            |

## Stage, QR, Reveal, And Route Freshness

| Done | ID      | Severity | Phase | Issue                                                                                                               | Closure evidence                                                                                                                                                                                                                                                                                             |
| ---- | ------- | -------- | ----- | ------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [x]  | UXR-005 | P1       | 1     | Stage QR code is not centered in its panel.                                                                         | Phase 1 e2e evidence added QR-panel center assertions in `tests/e2e/full-flow.spec.ts`, `tests/e2e/projector-mobile-evidence.spec.ts`, and `tests/phase9/fixtures/phase11-visual-evidence.ts`; `rtk npm run test:e2e` passed with 720p/desktop projector screenshots.                                        |
| [x]  | UXR-006 | P2       | 0     | Local/rehearsal QR can encode relative `/room` when `NEXT_PUBLIC_SITE_URL` is missing outside production.           | Phase 0 evidence: live `/stage` QR target was `https://bite-open-card-draw.vercel.app/room`, local `.env.local` sets the same public origin, production helper tests reject missing/localhost origins, and release/deployment checklists now require deployed absolute `/room` QR evidence before venue use. |
| [x]  | UXR-007 | P1       | 2     | Stage reveal cadence can stutter or look inconsistent because refresh timing and reveal timing are tightly coupled. | Phase 2 added tiebreak-aware stage refresh deferral and a separate reveal refresh cadence; `rtk npm run test:e2e` passed with Playwright assertions on `stage-auto-refresh` deferral and tiebreak reveal completion.                                                                                         |
| [x]  | UXR-008 | P1       | 2     | Phones can show final charts before the projector visibly reaches the final reveal.                                 | Phase 2 split stage-final display from public release: phones, `/charts`, and `/results` remain in the holding state until the host clicks `Confirm Stage Reveal Complete`; `rtk npm run test:e2e` captured `uxr-008-*` holding screenshots and passed.                                                      |
| [x]  | UXR-009 | P1       | 2     | Final `/stage`, `/vote`, `/charts`, and `/results` views can go stale after correction, reset, or round advance.    | Phase 2 keeps final public routes refreshing and `rtk npm run test:e2e` proved already-open `/stage`, `/vote`, `/charts`, and `/results` update after final release, result correction, reset, and round advance without manual browser refresh.                                                             |
| [x]  | UXR-010 | P1       | 3     | Standard stage chart cards are too small at 720p/projector scale.                                                   | Phase 3 increased stage-card hierarchy and tightened projector chrome; `rtk npm run test:e2e` passed 1280x720/1366x768/1920x1080 stage geometry with title, artist, difficulty, card-height, no-overlap, QR, and no-scroll assertions plus `pfr-031-stage-1280x720-voting.png`.                              |
| [x]  | UXR-011 | P3       | 4     | Stage error state has only manual retry and can strand the projector if nobody is watching the browser.             | Phase 4 added `/stage` error-boundary auto retry plus clearer projector recovery copy and runbook instructions; `rtk npm run test -- src/app/stage/error.test.ts` verifies the auto-retry and manual recovery UI.                                                                                            |

## Room, Vote, Charts, And Results

| Done | ID      | Severity | Phase | Issue                                                                                                                | Closure evidence                                                                                                                                                                                                                                                                                             |
| ---- | ------- | -------- | ----- | -------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [x]  | UXR-012 | P2       | 4     | `/room` gives no current tournament state before users choose player voting or view-only charts.                     | Phase 4 made `/room` dynamic with current round/status context and light auto-refresh while preserving `I am a player voting` and `View charts only`; `rtk npm run test:e2e -- tests/e2e/mobile-routes.spec.ts` passed and captured `uxr-012-mobile-room-awaiting-draw.png`.                                 |
| [x]  | UXR-013 | P2       | 3     | `/vote` waiting state gives little guidance beyond waiting for the host.                                             | Phase 3 rewrote `/vote` waiting guidance; `rtk npm run test:e2e` captured `uxr-013-mobile-vote-waiting-not-drawn.png`, `uxr-013-mobile-vote-waiting-not-open.png`, `uxr-013-mobile-vote-paused.png`, and `uxr-013-mobile-vote-closed-revealing.png`.                                                         |
| [x]  | UXR-014 | P2       | 3     | `No bans for this set` is easy to miss because it appears below all chart cards.                                     | Phase 3 moved the exact `No bans for this set` choice above the ballot grid; mobile e2e asserts empty selection cannot advance, no vague skip exists, checking no-bans enables progress, and selecting a ban clears no-bans.                                                                                 |
| [x]  | UXR-015 | P2       | 3     | Edit-after-save state does not clearly distinguish a saved server ballot from unsaved edits.                         | Phase 3 added server-confirmed saved copy and an unsaved-edit banner; `rtk npm run test:e2e` covers saved ballot, edit draft, forced failed save, previous server-confirmed reassurance, and reload preserving revision 2.                                                                                   |
| [x]  | UXR-016 | P2       | 3     | Duplicate-device/duplicate-ballot warning appears late in the identity flow.                                         | Phase 3 checks existing ballots on username selection and claims voter presence through the existing server action at Confirm; `rtk npm run test:e2e` asserts duplicate-ballot and active-device warnings appear before any ballot cards are shown on the second device, then a second Confirm can continue. |
| [x]  | UXR-017 | P3       | 4     | Public `/charts` copy includes reroll/ballot invalidation language that is irrelevant or confusing for spectators.   | Phase 4 rewrote `/charts` ready/waiting copy as spectator-safe event language; mobile e2e asserts no reroll, invalidation, computation, or snapshot wording on public routes.                                                                                                                                |
| [x]  | UXR-018 | P3       | 4     | One-set-drawn `/charts` state can be contradictory: copy says charts appear after both sets, but one set can render. | Phase 4 added distinct no-set, one-set, and both-sets `/charts` copy; `rtk npm run test:e2e -- tests/e2e/mobile-routes.spec.ts` captured `uxr-018-mobile-charts-one-set-drawn.png` and full-flow asserts the one-set status.                                                                                 |
| [x]  | UXR-019 | P2       | 4     | `/results` previous-round fallback can be misread as current-round results.                                          | Phase 4 added current-round pending and previous-round notice panels; e2e captured `uxr-019-mobile-results-pending.png` and `uxr-019-results-previous-round-fallback.png`.                                                                                                                                   |
| [x]  | UXR-020 | P3       | 4     | Result holding and vote-copy states include internal implementation language such as computation/snapshot wording.   | Phase 4 replaced public `/vote`, `/charts`, and `/results` implementation wording with event-language copy; mobile e2e negative assertions cover reroll, invalidation, computation, and snapshot wording.                                                                                                    |
| [x]  | UXR-021 | P3       | 4     | Mobile `/charts` navigation depends on hydration and can resemble a voting flow.                                     | Phase 4 makes `/charts` identify as a view-only chart browser, exposes anchor tab targets, keeps panels server-visible until hydration, avoids restoring unavailable partial-draw tabs, and e2e verifies no username or ballot controls appear.                                                              |
| [x]  | UXR-022 | P3       | 3     | Some phone controls are small for tournament-floor touch use.                                                        | Phase 3 increased secondary phone controls and added mobile tap-target assertions for room links, chart tabs, confirm, no-bans, next/review/submit, and saved edit buttons in `tests/e2e/mobile-routes.spec.ts`; `rtk npm run test:e2e` passed.                                                              |
| [x]  | UXR-023 | P3       | 4     | Routes share the app-wide browser title, reducing operator confidence across multiple tabs.                          | Phase 4 added a metadata title template plus route titles for `/room`, `/stage`, `/vote`, `/charts`, `/results`, and `/coolguy69`; mobile and full-flow e2e assert the route-specific browser titles.                                                                                                        |

## Admin Console And Host Workflow

| Done | ID      | Severity | Phase | Issue                                                                                                                                       | Closure evidence                                                                                                                                                                                                                                                                                      |
| ---- | ------- | -------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x]  | UXR-024 | P1       | 1     | `/coolguy69` panel order is not event-day focused.                                                                                          | Phase 1 e2e `expectAdminEventDayFlow` verifies visual order: host control, readiness, draw current round, reveal drawn charts, voting, manual correction, result reveal, then chart eligibility; screenshot `uxr-phase1-admin-event-day-flow.png` is attached by `rtk npm run test:e2e`.              |
| [x]  | UXR-025 | P1       | 1     | Draw/reveal/start-voting controls are not the main admin focus, and draw controls are buried below secondary panels.                        | `/coolguy69` now surfaces current-round draw controls, a `Reveal Drawn Charts` stage check, and voting controls before secondary setup; `rtk npm run test:e2e` passed the admin flow evidence.                                                                                                        |
| [x]  | UXR-026 | P1       | 1     | Chart Eligibility is open by default and pushes normal host actions far down the page.                                                      | Chart eligibility moved after day-of controls and the selected-pool details are closed by default; e2e asserts the details element is not open.                                                                                                                                                       |
| [x]  | UXR-027 | P1       | 1     | Reroll warning text is always visible, repeats per chart, and can spill across cramped columns.                                             | Current-round reroll actions now live in closed confirmation details using `DangerousActionDialog`; e2e asserts the long chart-reroll consequence is hidden until opened and verifies the summary before password entry.                                                                              |
| [x]  | UXR-028 | P2       | 1     | Manual ballot correction is placed after result reveal controls, increasing the chance a host computes results before noticing corrections. | `/coolguy69` now places `Manual Ballot Correction` visually before `Result Reveal Controls`; e2e asserts manual correction appears before compute/reveal controls.                                                                                                                                    |
| [x]  | UXR-029 | P2       | 5     | Host lock status is visible but not operator-clear enough for live use.                                                                     | Phase 5 added active/read-only/no-host context, short owner-session display, takeover/expiry guidance, and visible heartbeat confidence. `rtk npm run test:e2e` asserts the no-host, active-host, and second-browser read-only host-lock UI and captures admin evidence.                              |
| [x]  | UXR-030 | P2       | 5     | Live counts cannot be collapsed once revealed and can expose long chart-name overflow.                                                      | Phase 5 added `Hide live counts`, kept rows absent when hidden, verified show/refresh/hide in `rtk npm run test:e2e`, and captured `uxr-030-admin-live-counts-long-name.png` with deterministic long live-count text contained.                                                                       |
| [x]  | UXR-031 | P2       | 1     | Roster/config/release readiness details are too low or mixed into secondary areas for event setup confidence.                               | `/coolguy69` now has a top `Current Round Readiness` panel with host lock, active player count, current-round draw count, required-pool readiness, and local image-cache metadata; `rtk npm run test:e2e` verifies it in the admin flow evidence.                                                     |
| [x]  | UXR-032 | P3       | 5     | Admin roster, draw, and live-count tables/lists risk overflow with long usernames or chart names.                                           | Phase 5 converted risky admin lists/forms to contained responsive layouts and constrained native selects. `rtk npm run test:e2e` captures `uxr-032-admin-desktop-long-names.png`, `uxr-032-admin-narrow-long-names.png`, and `uxr-032-admin-draw-controls-long-name.png` with containment assertions. |
| [x]  | UXR-033 | P2       | 5     | Public `/charts` receives full `DrawRecord` data including draw metadata that is not needed for the visible public UI.                      | Phase 5 added a display-safe `/charts` view model and updated client props. `rtk npm run test -- src/lib/charts/public-chart-view.test.ts` proves draw metadata/snapshot/reason/source fields are omitted; full e2e verifies `/charts` still renders.                                                 |

## Closure Gate

- [ ] Every `UXR-*` item above is checked or explicitly accepted as-is by the user.
- [ ] `docs/phase-status.md` records the phases completed, checks run, risks, and assumptions.
- [ ] Browser screenshots cover desktop stage/admin and mobile room/vote/charts/results.
- [ ] Full tournament rehearsal evidence still satisfies the 48 -> 36 -> 24 -> 12 active-player requirement.
- [ ] Release checklist references this checklist before event use.
